<header>
	<h1>Update Article</h1>
</header>

<%= form_for(@article) do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %><br>

	<%= f.label :body %>
	<%= f.text_area :body %><br>

	<%= f.submit 'Submit' %>
<% end %>
